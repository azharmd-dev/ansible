- name: installing packages 
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: installing application # nginx mysql zip
    ansible.builtin.package:
      name: "{{ item }}"
      state: latest
    loop:
    - nginx
    - mysql
    - zip