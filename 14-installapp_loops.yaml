- name: installing packages 
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: installing application "{{ item }}"
    ansible.builtin.package:
      name: "{{ item }}"
      state: latest
    loop:
    - nginx
    - mysql
    - zip